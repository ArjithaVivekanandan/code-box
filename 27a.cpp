#include<iostream.h>
#include<conio.h>
#include<ctype.h>
bool isNumber(String s)
{
for(int j=0;j<s.length();i++)
if(isdigit(s[j])==0))
return true;
}
void main()
{
String str;
cout<<"Enter string:";
cin>>str;
if(isNumber(str)==0)
{
cout<<"Numeric number";
}
else
{
cout<<"Not numeric number";
}
getch();
}
